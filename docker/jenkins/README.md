# Jenkins image for YAS

Dockerfile build image Jenkins có sẵn: Maven 3.9, JDK 21, Docker CLI, Gitleaks.

Dùng cùng `docker-compose.jenkins.yml` ở root repo: `docker compose -f docker-compose.jenkins.yml up -d`.
