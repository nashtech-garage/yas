name: location service ci

on:
  push:
    paths:
      - "location/**"
  pull_request:
    paths:
      - "location/**"
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/java-service-ci-reusable.yml
    with:
      service: location
      service_label: Location
    secrets: inherit
