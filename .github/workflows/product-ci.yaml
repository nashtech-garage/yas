name: product service ci

on:
  push:
    paths:
      - "product/**"
  pull_request:
    paths:
      - "product/**"
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/java-service-ci-reusable.yml
    with:
      service: product
      service_label: Product
    secrets: inherit
