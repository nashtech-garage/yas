name: media service ci

on:
  push:
    paths:
      - "media/**"
  pull_request:
    paths:
      - "media/**"
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/java-service-ci-reusable.yml
    with:
      service: media
      service_label: Media
    secrets: inherit
