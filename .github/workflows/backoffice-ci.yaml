name: backoffice-ci

on:
  push:
    branches: ['**']
    paths:
      - 'backoffice/**'
      - '.github/workflows/actions/action.yaml'
      - '.github/workflows/backoffice-ci.yaml'
  pull_request:
    branches: [main]
    paths:
      - 'backoffice/**'
      - '.github/workflows/actions/action.yaml'
      - '.github/workflows/backoffice-ci.yaml'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true


jobs:
  test:

    permissions:
      contents: write
      security-events: write
      packages: write
      pull-requests: write
      checks: write
    name: Test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      FROM_ORIGINAL_REPOSITORY: ${{ github.event.pull_request.head.repo.full_name == github.repository || github.ref == 'refs/heads/main' }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci --ignore-scripts
        working-directory: backoffice

      - name: Install Linux SWC binary
        run: npm install --cpu=x64 --os=linux @next/swc-linux-x64-gnu --no-save --ignore-scripts
        working-directory: backoffice

      - name: Run Build (Checking for build errors)
        run: npm run build
        working-directory: backoffice

      - name: Run Lint
        run: npm run lint
        working-directory: backoffice

      - name: Run Prettier check
        run: npx prettier --check .
        working-directory: backoffice

      - name: Run npm audit
        run: npm audit --omit=dev
        continue-on-error: true
        working-directory: backoffice



  build:

    permissions:
      contents: write
      security-events: write
      packages: write
      pull-requests: write
      checks: write
    name: Build
    needs: [test]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          fetch-depth: 0

      - name: Compute image tags
        id: meta
        run: |
          SHORT_SHA=$(git rev-parse --short=7 HEAD)
          BRANCH_TAG=$(echo "${GITHUB_REF_NAME}" | sed 's/[^a-zA-Z0-9._-]/-/g')
          echo "short_sha=${SHORT_SHA}" >> $GITHUB_OUTPUT
          echo "branch_tag=${BRANCH_TAG}" >> $GITHUB_OUTPUT

      - name: Log in to Docker Hub
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push Docker image
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6
        with:
          context: ./backoffice
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/yas-backoffice:${{ steps.meta.outputs.short_sha }}
            ${{ secrets.DOCKERHUB_USERNAME }}/yas-backoffice:${{ steps.meta.outputs.branch_tag }}
            ${{ github.ref_name == 'main' && format('{0}/yas-backoffice:latest', secrets.DOCKERHUB_USERNAME) || '' }}
          cache-from: type=gha,scope=${{ github.workflow }}
          cache-to: type=gha,mode=max,scope=${{ github.workflow }}
