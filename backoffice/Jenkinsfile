/**
 * Backoffice Service CI/CD Build Functions
 * 
 * This file contains build logic that can be loaded by master orchestrator.
 * For standalone pipeline, see backoffice/Jenkinsfile.standalone (if needed)
 */

// ─── Build Logic ────────────────────────────────────────────────────────────────
def runBuild(String serviceName) {
    echo ">>> Checkout"
    checkout scm

    echo ">>> Install Dependencies"
    dir(serviceName) {
        sh 'npm ci'
    }

    echo ">>> Build"
    dir(serviceName) {
        sh 'npm run build'
    }

    echo ">>> Test"
    dir(serviceName) {
        sh 'npx jest --coverage --ci --reporters=default --reporters=jest-junit || true'
    }
    junit testResults: "${serviceName}/junit.xml", allowEmptyResults: true
    publishHTML([
        allowMissing: true,
        alwaysLinkToLastBuild: true,
        keepAll: true,
        reportDir: "${serviceName}/coverage/lcov-report",
        reportFiles: 'index.html',
        reportName: "${serviceName.capitalize()} Coverage Report"
    ])

    echo ">>> Lint"
    dir(serviceName) {
        sh 'npm run lint'
    }

    echo ">>> Prettier Check"
    dir(serviceName) {
        sh 'npx prettier --check .'
    }

    echo ">>> Audit"
    dir(serviceName) {
        sh 'npm audit --omit=dev || true'
    }

    if (env.BRANCH_NAME == 'main') {
        echo ">>> Docker Build & Push"
        withCredentials([usernamePassword(
            credentialsId: 'docker-registry-credentials',
            usernameVariable: 'DOCKER_USER',
            passwordVariable: 'DOCKER_PASS'
        )]) {
            sh """
                DOCKER_REGISTRY='ghcr.io'
                DOCKER_IMAGE="\${DOCKER_REGISTRY}/nashtech-garage/yas-${serviceName}"
                echo "\$DOCKER_PASS" | docker login \${DOCKER_REGISTRY} -u "\$DOCKER_USER" --password-stdin
                docker build -t \${DOCKER_IMAGE}:latest -t \${DOCKER_IMAGE}:${BUILD_NUMBER} ./${serviceName}
                docker push \${DOCKER_IMAGE}:latest
                docker push \${DOCKER_IMAGE}:${BUILD_NUMBER}
                docker logout \${DOCKER_REGISTRY}
            """
        }
    }
}

// Export for loading
return this
