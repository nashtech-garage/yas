apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: inventory-vs
  namespace: dev
spec:
  hosts:
    - inventory
  http:
    - route:
        - destination:
            host: inventory
      retries:
        attempts: 3
        perTryTimeout: 2s
        retryOn: 5xx,connect-failure,refused-stream
      timeout: 10s
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: inventory-vs
  namespace: staging
spec:
  hosts:
    - inventory
  http:
    - route:
        - destination:
            host: inventory
      retries:
        attempts: 3
        perTryTimeout: 2s
        retryOn: 5xx,connect-failure,refused-stream
      timeout: 10s
