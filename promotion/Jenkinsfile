pipeline {
    agent any

    tools {
        maven 'Maven'   // phải config trong Jenkins Global Tool
        jdk 'JDK11'
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Test') {
            steps {
                dir('promotion') {
                    sh 'mvn clean test'
                }
            }
            post {
                always {
                    // Upload test result
                    junit 'promotion/target/surefire-reports/*.xml'

                    // Upload coverage (JaCoCo)
                    publishCoverage adapters: [
                        jacocoAdapter('media-service/target/site/jacoco/jacoco.xml')
                    ]
                }
            }
        }

        stage('Build') {
            steps {
                dir('promotion') {
                    sh 'mvn clean package -DskipTests'
                }
            }
        }
    }
}
