# Spec Kit Artifacts Moved

Canonical Spec Kit artifacts for Lab 2 CD now live in the CD repository:

```text
git@github.com:emanhthangngot/yas-cd.git
```

Use `yas-cd/specs/001-yas-lab2-cd/**` for the active `spec.md`, `plan.md`, and `tasks.md`.

This pointer exists so app-repo contributors do not edit stale copies of the CD specification.
