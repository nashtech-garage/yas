apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: observability-dashboard
spec:
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  json: |
{{ .Files.Get "observability_dashboard.json" | indent 4 }}
