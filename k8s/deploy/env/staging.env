# Môi trường STAGING — deploy bản release đã pin (Mục 6: job staging → namespace staging)
NAMESPACE=staging
DOMAIN=staging.yas.local.com
# Lưu ý: CI (người B) phải push tag release này thì staging mới có ý nghĩa.
IMAGE_TAG=stable
IMAGE_REGISTRY=
