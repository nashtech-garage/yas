# Môi trường DEV — deploy bản main mới nhất (Mục 6: job dev → namespace dev)
NAMESPACE=dev
DOMAIN=dev.yas.local.com
IMAGE_TAG=latest
# Tùy chọn: prefix registry+owner để ghi đè image mặc định của chart.
# Để trống = dùng image upstream (ghcr.io/nashtech-garage/yas-*).
IMAGE_REGISTRY=
