# ============================================================
# DestinationRule - Enforce mTLS cho tất cả services trong YAS
# ============================================================
# DestinationRule định nghĩa policies áp dụng cho traffic
# SAU KHI routing đã xảy ra. Ở đây ta dùng để enforce mTLS
# ở phía client (caller) khi gọi tới các services.
#
# trafficPolicy.tls.mode: ISTIO_MUTUAL
#   - Istio tự động quản lý certificates
#   - Sử dụng certificates do Istio CA (citadel) cấp phát
#   - Không cần cấu hình certificate thủ công
#
# Kết hợp với PeerAuthentication STRICT:
#   PeerAuthentication = server-side (yêu cầu client phải gửi mTLS)
#   DestinationRule    = client-side (cấu hình client gửi mTLS)
# ============================================================

# --- DestinationRule mặc định cho toàn bộ namespace ---
# Áp dụng cho mọi service trong namespace yas thông qua wildcard
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: yas-default-mtls
  namespace: yas
  labels:
    app.kubernetes.io/part-of: yas
    app.kubernetes.io/managed-by: istio
spec:
  host: "*.yas.svc.cluster.local"
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL

---
# --- DestinationRule riêng cho từng service (chi tiết hơn) ---
# Các rule riêng dưới đây là optional, vì rule wildcard ở trên
# đã cover tất cả. Tuy nhiên, giữ lại để dễ customize sau này
# (ví dụ: thêm connection pool, load balancing policy cho từng service)

apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: product-mtls
  namespace: yas
spec:
  host: product.yas.svc.cluster.local
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL
    connectionPool:
      http:
        h2UpgradePolicy: DEFAULT
        maxRequestsPerConnection: 100
      tcp:
        maxConnections: 100

---
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: cart-mtls
  namespace: yas
spec:
  host: cart.yas.svc.cluster.local
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL
    connectionPool:
      http:
        maxRequestsPerConnection: 100
      tcp:
        maxConnections: 100

---
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: order-mtls
  namespace: yas
spec:
  host: order.yas.svc.cluster.local
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL
    connectionPool:
      http:
        maxRequestsPerConnection: 100
      tcp:
        maxConnections: 100

---
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: customer-mtls
  namespace: yas
spec:
  host: customer.yas.svc.cluster.local
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL

---
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: payment-mtls
  namespace: yas
spec:
  host: payment.yas.svc.cluster.local
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL

---
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: inventory-mtls
  namespace: yas
spec:
  host: inventory.yas.svc.cluster.local
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL

---
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: media-mtls
  namespace: yas
spec:
  host: media.yas.svc.cluster.local
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL

---
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: location-mtls
  namespace: yas
spec:
  host: location.yas.svc.cluster.local
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL

---
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: tax-mtls
  namespace: yas
spec:
  host: tax.yas.svc.cluster.local
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL

---
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: promotion-mtls
  namespace: yas
spec:
  host: promotion.yas.svc.cluster.local
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL

---
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: rating-mtls
  namespace: yas
spec:
  host: rating.yas.svc.cluster.local
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL

---
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: search-mtls
  namespace: yas
spec:
  host: search.yas.svc.cluster.local
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL

---
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: recommendation-mtls
  namespace: yas
spec:
  host: recommendation.yas.svc.cluster.local
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL

---
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: webhook-mtls
  namespace: yas
spec:
  host: webhook.yas.svc.cluster.local
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL

---
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: storefront-bff-mtls
  namespace: yas
spec:
  host: storefront-bff.yas.svc.cluster.local
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL

---
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: backoffice-bff-mtls
  namespace: yas
spec:
  host: backoffice-bff.yas.svc.cluster.local
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL

---
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: nginx-mtls
  namespace: yas
spec:
  host: nginx.yas.svc.cluster.local
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL
