# Unsafe Examples (Quarantined)

This directory contains intentionally vulnerable Java examples for static analysis testing. It is NOT part of the Maven build and MUST NEVER be executed.

- Not referenced by the root pom modules
- Do not import or run these classes
