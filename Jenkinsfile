pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Run Build on test branch'
            }
        }

        stage('Test') {
            steps {
                echo 'Run Test'
            }
        }
    }
}
